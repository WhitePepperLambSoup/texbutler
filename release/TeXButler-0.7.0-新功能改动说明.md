# TeXButler 0.7.0 新功能与改动说明

- 版本：`0.7.0`
- 功能代码范围：`951fba5..54a6b25`（130 笔功能、修复、测试与设计提交）
- 当前分支：`main`
- 说明日期：2026-08-11

## 重点更新

### 1. AI 对话工作区重新设计

- 重新整理 AI 面板的消息区、输入区、工具栏和操作菜单布局，解决控件错位、遮挡、间距失衡及视觉层级混乱的问题。
- 每个打开的 TeX 文件拥有独立 AI 对话；切换或打开新文件时自动进入该文件对应的对话，已有文件对话不会被关闭。
- 文件级 AI 对话会持久化保存，软件退出并再次打开项目后仍可恢复。
- AI 会话按项目与文件路径隔离，避免同名文件或跨项目切换时串用上下文。
- 改进 AI 修复流程的文件定位：工具可识别当前项目中的相对路径、绝对路径以及日志中带项目目录的路径，不再把 `read_file` 当作未知工具而直接终止修复。
- 修复快速切换项目时异步 AI、编译及文件刷新结果覆盖当前项目状态的问题。
- 一键诊断和修复会先聚焦错误所属文件，最多执行三轮；空补丁和没有实际变化的 hunk 不会写盘或触发无意义编译。
- fenced JSON 工具调用增加明确编辑意图门控：工具教程、演示、调用格式和 schema 示例不会被当作真实编辑执行。
- AI 文件工具只允许访问项目内受支持的文档类型，并拒绝越界、歧义和非本机 Windows 盘符路径。
- 统一项目与编译结果锁顺序，修复项目切换同时执行 AI 修复时可能发生的锁反转。

### 2. 新建文件与模板流程

- 恢复并明确“新建文件”入口：文件始终创建在当前打开目录中，无需重复填写完整文件路径。
- 模板市场用于创建新文件，不再错误地强制进入“新建项目”流程。
- 新建文件对话框支持文件名、扩展名、模板选择和目标目录提示，并对无效名称及路径进行校验。
- 新建文件后自动打开编辑器，并自动建立该文件对应的 AI 对话。
- 模板市场扩充至 169 项，覆盖高校论文、海外院校、期刊、幻灯片、海报、简历、信件、报告和课程作业。
- 8 个内置模板完成真机编译验证；下载模板会进行文档结构验证并显示可信状态。
- 导入失败时仅回滚本次新建的文件和目录，保留项目原有内容。

### 3. UI 布局、外观与可读性

- 修复主界面多个区域的遮挡、挤压和尺寸计算问题，优化窄窗口下的布局表现。
- 将编辑器次要工具收进溢出菜单，避免工具按钮与文件标签互相覆盖。
- 修复“调整外观”和液态玻璃设置栏无法点击的问题。
- 降低“更多编辑工具”和“更多 AI 操作”弹层的透明度，增强背景分离度。
- 调整浅色、深色及玻璃效果下的文字、图标、边框和背景对比度，解决文字颜色与背景过近导致看不清的问题。
- 优化菜单、弹层、按钮、输入框、空状态和错误提示的视觉一致性。

### 4. 面板拖动体验

- 修复拖动分隔条后鼠标仍被捕获的“粘连”问题；松开鼠标、指针取消或窗口失焦时均会可靠结束拖动。
- 统一水平与垂直分隔条的尺寸计算，修复面板调整方向与鼠标移动方向相反的问题。
- 增加尺寸边界保护，避免拖动后侧栏、编辑器、AI 面板或 PDF 预览被压缩到不可用状态。

### 5. PDF 预览恢复

- 恢复 PDF 预览面板及相关布局状态。
- 修复面板切换、项目切换和重新编译后 PDF 区域消失或被其他区域覆盖的问题。
- 保留编辑器、PDF 与 AI 面板之间的可调布局能力。

### 6. 编译与项目状态可靠性

- 为项目和文档路径增加统一规范化处理，减少 Windows 路径、相对路径和日志路径不一致造成的文件读取失败。
- 修复跨项目编译、刷新和异步响应返回顺序不一致时，旧项目结果覆盖新项目的问题。
- 改进项目树、新建文件、编译结果与 AI 当前文件之间的同步。
- 编译前自动保存所有 dirty 标签，使编译内容与编辑器当前显示完全一致。
- 修复多文件项目主文件扫描返回绝对路径后选错编译入口的问题。
- 系统 TeX 编译同时解析 `.log` 与 stdout/stderr，保留 XeLaTeX 等引擎的真实错误位置和原始证据。
- 未知行号保持为空，不再显示伪造的第 0 行；仅有 warning 的异常退出也会生成可操作错误。

## 验证情况

合并到 `main` 后已重新完成以下验证：

- TypeScript 类型检查通过。
- 前端生产构建通过。
- Rust 核心测试：286 项通过、0 项失败、1 项因需要系统 TeX 环境而忽略；日志集成测试 2 项通过。
- 7 项需要真实 API、网络或系统 TeX 的集成测试按设计保持忽略。

功能分支交付前还完成了以下验证：

- WebView2 端到端测试通过：`v074`、`v081`、`v084`、`v085`、`v086`、`v087`。
- 覆盖新建文件、模板、AI 文件级会话、重启恢复、AI 工具路径、外观菜单、PDF 面板、分隔条拖动和跨项目异步竞态等关键流程。

## 测试安装包

包含本轮改动的测试安装包位于：

`release/0.7.0c/`

- `TeXButler_0.7.0c_x64-setup.exe`
- `TeXButler_0.7.0c_x64_en-US.msi`
- `SHA256SUMS.txt`
- `BUILD-INFO.txt`

文件名中的 `c` 是 0.7.0 的第三个测试候选构建标识。两个安装包均未进行代码签名，Windows 可能显示未知发布者提示；最终 SHA-256 以同目录 `SHA256SUMS.txt` 为准。
